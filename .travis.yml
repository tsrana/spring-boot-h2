language: java

services:
  - docker

before_install:
  - docker build -t h2-image .
  - docker run -d --name h2 -p 8080:8080 h2-image
  - docker ps -a
  - docker logs --tail 50 h2
  

script:
  - java --version
  - docker logs --tail 50 h2